[project]
name = "fayan-ai"
builder = "nixpacks"
build_command = "cd code && pip install -r requirements.txt"
start_command = "cd code && uvicorn fayan_main:app --host 0.0.0.0 --port ${PORT:-8000}"

[nixpacks.variables]
PORT = "8000"
